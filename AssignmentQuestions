1. When compared to ArrayLists, LinkedLists should be used when there is a large number of additions or deletions. Explain why this is the case
Because they can change easily and store a great amount of objects.

2. What is the internal data structure of an ArrayList?
An ArrayList stores the data of an object class internally in an array.

3. What is the internal data structure of a LinkedList?
A LinkedList stores added elements using Node objects.

4. To what java package do all Java Collection Framework classes belong?
java.util

5.What Book method gets executed when the List method contains is used?
The equals method to see if the object matches any within the Book.